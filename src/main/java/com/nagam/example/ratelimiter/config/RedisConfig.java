package com.nagam.example.ratelimiter.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class RedisConfig {
    // Uses Spring Boot auto-configuration for Redis
}
